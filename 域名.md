# 站点域名管理

全站的站点域名统一由仓库根目录的 **`.env`** 文件里的 `SITE_DOMAIN` 控制，**这是唯一真源**。

```env
SITE_DOMAIN=lyuu.eu.cc
```

> ⚠️ **不要在本文件里改域名**，请改 `.env`。本文件仅作说明。

## 改域名怎么做

1. 编辑根目录 `.env`，把 `SITE_DOMAIN` 改成新域名（只写裸域名，不要带 `http://` 和结尾斜杠）。
2. 提交并推送到 `lyuu_cn` 分支。GitHub Actions 会自动运行 `set-domain.js`：
   - 同步根 `index.html` 里的 3 个站内链接（GPNU资源 / 简历 / 关于）
   - 同步根 `gpnu.html` 里遗留的旧域名 `lyuu.cn` 站内资源链接（PDF 等），归一为 `SITE_DOMAIN`
   - 同步 `hexo/_config.next.yml` 的 `portal` 菜单
   - 同步 `hexo/_config.yml` 的 `url`（博客全站 URL 由它驱动，CI 重建后所有 `blog/` 页面域名自动更新）
   - 覆写 `CNAME`
3. 约 1 分钟后 GitHub Pages 生效。

## 涉及文件

| 文件 | 作用 |
|---|---|
| `.env` | **域名唯一真源**（改这里） |
| `set-domain.js` | 同步脚本（CI 调用，本地也可跑） |
| `index.html` | 主页站内链接（被同步，勿手改这 3 处） |
| `gpnu.html` | GPNU 资源页站内资源链接（被同步，原 `lyuu.cn` 旧域名已归一为 `SITE_DOMAIN`） |
| `hexo/_config.next.yml` | `portal` 菜单（被同步） |
| `hexo/_config.yml` | `url`（博客真源，被同步） |
| `CNAME` | GitHub Pages 自定义域名（被覆写） |

## 为什么不用「把变量写进 .md」

`.md` 是给人看的文档，构建（Hexo / CI）和浏览器都不会解析它做变量替换，写在里面的变量不会传播到任何 HTML。
所以这里用结构化的 `.env` 当真源，由 `set-domain.js` 在构建期统一替换——这才是真正「改一处、全站更新」的做法。
