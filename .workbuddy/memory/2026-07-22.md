# 2026-07-22 工作日志

## 在 java-walker.github.io 仓库搭建独立 Hexo 博客
- 用户目标：全新独立博客站，技术栈选 Hexo（环境有 Node 22.22.2，没装 Go），要求「先只搭框架、别动现有文件」。
- 在仓库子目录 `hexo/` 初始化 Hexo（hexo-cli 4.3.2）。GitHub git clone hexo-starter 连接被 reset 失败，自动改用本地模板复制（故 themes/ 初始为空）。
- 主题：NexT 8.28.0，npm 方式安装 `hexo-theme-next`（npm registry 可达；GitHub 直连不稳定）。已配 `_config.next.yml`（Gemini scheme、菜单、社交、代码复制按钮）。
- 主配置 `_config.yml`：title=Lyuu 的博客、url=https://lyuu.cc、lang=zh-CN、timezone=Asia/Shanghai、theme=next；deploy=git 到 main 分支（已加「覆盖根目录现有 index.html/ai-guide/gpnu.html」安全警告）。
- 内容：2 篇示例文章 + about 页面；删除默认 hello-world；`hexo generate` 成功（57 文件，EXIT 0）。
- 本地预览 `hexo server :4000` 已启动（后台任务 Lh7kOv）。
- 部署注意：GitHub Pages 用户站点发布目录为 main 分支根目录，直接 `hexo deploy` 会覆盖现有内容；建议源码放 source 分支 + GitHub Actions 构建部署。详见 hexo/README.md。
- 环境坑：全局 `.npmrc` 写操作被 EPERM 拒绝（权限/锁定），避免 `npm config set` 写文件，改用不写文件的 npm 操作。npm registry 可达，GitHub 直连不稳。

## 2026-07-23 复核：GitHub 直连仍不通
- 重新 `git ls-remote` / `git clone hexo-starter` 仍报 `curl 35 Recv failure: Connection was reset`，github.com HTTPS 直连持续被重置。
- 结论：本机无法直连 GitHub，故本机 `hexo deploy`(push) 也不可行，必须用 GitHub Actions 服务端构建部署绕开；npm registry 不受影响。
